# This file contains the task configurations for the Financial Data Crew
# The crew is responsible for gathering and analyzing financial data for a company

comprehensive_financial_analysis_task:
  description: >
    You are researching content for the "Financial Deep Dive" section in a stock analysis
    report about the stock of the following company: 

    Company name: {company_name}.

    Company ticker: {company_ticker}.

    Website: {company_website}

    Industry: {industry}.

    Using the tools available to you and your knowledge sources, gather comprehensive financial data for {company_name} (ticker: {company_ticker}).
    Focus on populating ALL components of the `FinancialData` model:

    1. Income Statement Analysis:
        - Net income, total revenue, gross profit, operating income, and EBITDA.
        - Basic EPS, diluted EPS, revenue growth, earnings growth.
        - Gross margins, operating margin, and profit margin.

    2. Balance Sheet Analysis:
        - Total assets, total liabilities, and total equity.
        - Total debt, net debt, and cash and equivalents.
        - Working capital, current ratio, quick ratio, and debt-to-equity ratio.

    3. Cash Flow Analysis:
        - Total cash, total debt.
        - Free cash flow, operating cash flow, and capital expenditure.
        - Repayment of debt and debt issuance.
        - Cash changes, investing cash flow, and financing cash flow.
        - Repurchases of stock.

    4. Growth Metrics:
        - Revenue growth
        - Earnings growth
        - 5-year revenue growth rate
        - 2-year revenue growth rate
        - Free Cash Flow growth
    
    5. Valuation Metrics:
        - Market cap.
        - Trailing and forward P/E ratios.
        - Price-to-sales, price-to-book ratios.
        - Enterprise value, EV/Revenue, and EV/EBITDA ratios.
        - Trailing and forward EPS.

    6. Price Metrics:
        - Current price, 52-week high, 52-week low.
        - 50-day and 200-day moving averages.

    7. Return Metrics:
        - Return on equity, return on assets, and return on invested capital.

    8. Dividend Metrics:
        - Cash dividends paid.
        - Dividend yield, dividend rate, and payout ratio.
        - Ex-dividend date and next dividend date.

    Use all available tools systematically to gather this information.

    Current date (ISO format): {date_iso}.
  expected_output: >
    The complete `FinancialData` Pydantic model with ALL components fully populated:
    
    1. `income_statement_analysis`: Complete `IncomeStatementAnalysis` model with:
        - `net_income`, `total_revenue`, `gross_profit`, `operating_income`, `ebitda`
        - `basic_eps`, `diluted_eps`, `revenue_growth`, `earnings_growth`
        - `gross_margins`, `operating_margin`, `profit_margin`

    2. `balance_sheet_analysis`: Complete `BalanceSheetAnalysis` model with:
        - `total_assets`, `total_liabilities`, `total_equity`
        - `total_debt`, `net_debt`, `cash_and_equivalents`
        - `working_capital`, `current_ratio`, `quick_ratio`, `debt_to_equity`
    
    3. `cash_flow_analysis`: Complete `CashFlowAnalysis` model with:
        - `total_cash`, `total_debt`
        - `free_cash_flow`, `operating_cash_flow`, `capital_expenditures`
        - `repayment_of_debt`, `debt_issuance`
        - `cash_changes`, `investing_cash_flow`, `financing_cash_flow`
        - `repurchases_of_stock`

    4. `growth_metrics`: Complete `GrowthMetrics` model with:
        - `revenue_growth`, `earnings_growth`, `five_year_revenue_growth_rate`, `two_year_revenue_growth_rate`, `free_cash_flow_growth`

    5. `valuation_metrics`: Complete `ValuationMetrics` model with:
        - `market_cap`
        - `trailing_pe_ratio`, `forward_pe_ratio`
        - `price_to_sales`, `price_to_book`
        - `enterprise_value`, `enterprise_to_revenue`, `enterprise_to_ebitda`
        - `trailing_eps`, `forward_eps`

    6. `price_metrics`: Complete `PriceMetrics` model with:
        - `current_price`
        - `fifty_two_week_high`, `fifty_two_week_low`
        - `fifty_day_average`, `two_hundred_day_average`

    7. `return_metrics`: Complete `ReturnMetrics` model with:
        - `return_on_equity`, `return_on_assets`, `return_on_invested_capital`

    8. `dividend_metrics`: Complete `DividendMetrics` model with:
        - `cash_dividends_paid`
        - `dividend_yield`, `dividend_rate`, `payout_ratio`
        - `ex_dividend_date`, `next_dividend_date`

  agent: researcher

writing_task:
  description: >
    Draft a comprehensive and insightful "Financial Deep Dive" section of the stock analysis report,
    incorporating the detailed findings from the detailed financial analysis provided to you.
    The section should be structured as follows:- An introduction to the section
    - A sub-section on **Income Statement Analysis**, presenting data and insights from the `IncomeStatementAnalysis` model.
    - A sub-section on **Balance Sheet Analysis**, presenting data and insights from the `BalanceSheetAnalysis` model.
    - A sub-section on **Cash Flow Analysis**, presenting data and insights from the `CashFlowAnalysis` model.
    - A sub-section on **Growth Metrics**, presenting data and insights from the `GrowthMetrics` model.
    - A sub-section on **Valuation Metrics**, presenting data and insights from the `ValuationMetrics` model.
    - A sub-section on **Price Metrics**, presenting data and insights from the `PriceMetrics` model.
    - A sub-section on **Return Metrics**, presenting data and insights from the `ReturnMetrics` model.
    - A sub-section on **Dividend Metrics**, presenting data and insights from the `DividendMetrics` model.
    Each subsection should clearly present the data gathered, along with insightful analysis and context.
    Guidelines:
    {writer_guidelines}

    Current date: {date_us}.
  expected_output: >
    A complete draft of the "Financial Deep Dive" section including:
    - An introduction to the section
    - A sub-section on **Income Statement Analysis**.
    - A sub-section on **Balance Sheet Analysis**.
    - A sub-section on **Cash Flow Analysis**.
    - A sub-section on **Growth Metrics**.
    - A sub-section on **Valuation Metrics**.
    - A sub-section on **Price Metrics**.
    - A sub-section on **Return Metrics**.
    - A sub-section on **Dividend Metrics**.
    Each subsection should clearly present the data gathered, along with insightful analysis and context.
    You must begin the section with the heading: ## Financial Deep Dive
    The output must be formatted as markdown without code blocks.
  agent: writer

critic_task:
  description: >
    Perform a rigorous critical review of the draft "Financial Deep Dive" section, focusing on accuracy, clarity, coherence, completeness, and overall impact.
    {critic_guidelines}
  expected_output: >
    A detailed critique of the draft, including specific suggestions for improvement.
    You must also output the full, verbatim draft section content that you have critiqued as well.
    Current date (ISO format): {date_iso}.
  agent: critic

editing_task:
  description: >
    Refine and finalize the "Financial Deep Dive" section, incorporating the critic's feedback to achieve the highest possible quality.
    Guidelines:
    {editor_guidelines}
    - Make sure the section begins with the heading: ## Financial Deep Dive
    Current date: {date_us}.
  expected_output: >
    A final, polished version of the "Financial Deep Dive" section, ready for inclusion in the stock analysis report.
    This should be a fully revised and improved version, addressing all feedback from the critic while maintaining
    the clear structure defined by our financial data model.
    The output must be formatted as markdown without code blocks.
  agent: editor
